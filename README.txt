# Razorpay Starter

1. Run `npm install`
2. Copy `.env.example` to `.env` and put your Razorpay test key/secret.
3. Start server: `npm run dev`
4. Open http://localhost:3000 and click Pay ₹1 (test).
5. After success, MCQs will unlock by setting localStorage key.
